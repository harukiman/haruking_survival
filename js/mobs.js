// mobs.js — モブAI（MVPはスタブ。次波で実装）
window.Game = window.Game || {};

Game.Mobs = (function () {
  function update() {
    // 次波: スポーン規則・AI・戦闘・ドロップ
  }
  function draw(ctx) {
    // 次波: モブ描画
  }
  return { update, draw };
})();
